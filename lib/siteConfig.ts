/**
 * Single source of truth for values that change periodically.
 * Update AVAILABILITY_MONTH whenever the intake window shifts.
 */
export const AVAILABILITY_MONTH = "May"; // ← UPDATE THIS each month
